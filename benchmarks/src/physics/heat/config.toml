[heat.cpu.test]
code_target = "CPUTarget"
float_type = ["Float32", "Float64"]
resolution = [5, 2, 1]

[heat.cuda.test]
code_target = "CUDATarget"
float_type = ["Float32", "Float64"]
resolution = [5, 2, 1]
